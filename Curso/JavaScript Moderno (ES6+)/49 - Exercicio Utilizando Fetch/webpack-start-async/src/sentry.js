export default class Sentry {
  constructor(id, name, collisionMinYear, collisionMaxYear) {
    this.id = id
    this.name = name
    this.collisionMinYear = collisionMinYear
    this.collisionMaxYear = collisionMaxYear
  }
}